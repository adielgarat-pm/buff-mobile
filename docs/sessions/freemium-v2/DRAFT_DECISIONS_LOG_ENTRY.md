# DRAFT — Proposed DECISIONS_LOG entry (Freemium v2, as implemented)

> **⚠️ DRAFT בלבד, לאישור Adi. לא הוכנס ל-`docs/BUFF_DECISIONS_LOG.md`.**
> מחליף את `docs/research/DRAFT_DECISION_freemium_v2.md` (טיוטת המחקר) בגרסה שמשקפת את מה שאושר ויושם ב-2026-09-23.

---

### D-2026-09-23-02: Freemium v2 — הכל חינם חוץ מה-AI, ניסיון הפוך של 14 יום, עריכת מיקוד

**ההחלטה (אושרה ע"י Adi, 2026-09-23):**
- **חינם לתמיד:** משימות בלי מגבלה, ילדים בלי מגבלה, BUDDY וסקינים, BUFFs, חנות ומימוש, Vibe Check / SOS / Pause / Anchor, התראות, מערכת שעות ידנית **וגם ייבוא מערכת שעות ב-AI** (ההחלטה מ-2026-07-29 נשארת), פעילויות, הכנת תיק, יציאה משגרה, ועריכת מיקוד.
- **בתשלום — "המאמן של BUFF":** מאמן ה-AI / התובנות, וקליטת משימות מתמונה/טקסט (המארגן החכם). מחיר ללא שינוי: $9.99 לחודש, $59.99 לשנה. Founding-100 נשאר.
- **ניסיון הפוך:** 14 יום של המאמן, מרגע המשימה **האמיתית** הראשונה של הילד. משימת "עושים יחד" של האונבורדינג (`onboarding_first_task`) ושורות `seed` לא מתחילות אותו.
- **משפחות ותיקות:** משפחות שהשעון שלהן רק "מולא" במיגרציה 037 (בלי ניסיון שרץ בפועל, בלי פרימיום/לייפטיים) נפתחו מחדש — הניסיון יתחיל אצלן במשימה האמיתית הבאה. בפועל: 192 משפחות.
- **אחרי הניסיון:** תובנת AI חינמית אחת לכל ילד בכל שבוע.
- **רגעי הניסיון בדשבורד (פעם אחת כל אחד, רק להורה):** "המאמן פעיל ל-14 יום" / "נשארו עוד 4 ימים" / "14 הימים הסתיימו... נשאר חינם" עם [להמשיך עם המאמן] [סגור]. בלי מסגור של אובדן.
- **פייוול:** לעולם לא באונבורדינג, לעולם לא לילד. רק בפעולת AI של הורה אחרי הניסיון, ובהודעת הסיום הרכה.
- **עריכת מיקוד:** עריכת ילד → "מיקוד ופרסים". ההורה משנה את המיקוד, ומקבל הצעות למשימות/פרסים שעוד אין לילד — כולן לא מסומנות כברירת מחדל. שום דבר קיים לא נמחק או משתנה.

**הסיבה:** צוואר הבקבוק הוא אקטיבציה, לא המרה (ראו `docs/research/FREEMIUM_STRATEGY_2026-09.md`). ה-AI כמעט לא נראה — הניסיון ההפוך חושף אותו לכל משפחה שמתחילה לעבוד. עריכת המיקוד — מראיון עם משפחה d111: ההורה בחר מיקוד לא נכון באונבורדינג ולא יכל לתקן.

**מה נדחה:** פייוול קשה; ניסיון מההרשמה; השארת גייט ילד 2 ו-6 משימות; תשלום על ייבוא מערכת שעות (Q1).

**תנאי סיכון מוסכם מראש:** אם 60 יום אחרי ההשקה פחות מ-1% מהמשפחות שסיימו ניסיון משלמות — מוסיפים מנוף סקייל אחד למשפחות מעורבות, לא חיכוך מוקדם.

**יישום:** `docs/sessions/freemium-v2/` (SPEC, STATUS). מיגרציה 058 (הוחלה ב-prod), `generate-child-insights` v19 (נפרס).

**מחליף:** את הגייט על ילד 2, מגבלת המשימות ורשימת הפיצ'רים בתשלום מ-D-2026-06-19-01; את "ללא תקופת ניסיון" מ-D-2026-06-19-01.

**סטטוס:** DRAFT — ממתין ל-Adi להעתקה ל-DECISIONS_LOG.
