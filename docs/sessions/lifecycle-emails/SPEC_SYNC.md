# SPEC_SYNC — pkg/lifecycle-emails

Which canonical docs to update per phase (same commit as the code):

| Phase | Canonical docs to update |
|---|---|
| Phase 1 (index + unsubscribe fn) | none (infra only; this session's STATUS.md suffices) |
| Phase 2 (OAuth consent ask) | BUFF_GAP_ANALYSIS (propose to Adi — OAuth consent gap closes); RELEASE_QUEUE row (client change → build/OTA) |
| Phase 3 (sender + T2 armed) | INTEGRATION_LEARNINGS (first automated-send learnings); BUFF_MESSAGING (propose: lifecycle templates as canonical pitch copy) |
| Phase 4 (all triggers + cron) | DEV_SERVER_LIFECYCLE n/a; document cron job alongside BUDDY EOD job reference |

Adi-owned docs (DECISIONS_LOG, GAP_ANALYSIS, VALUES) — propose only, never edit unilaterally.
