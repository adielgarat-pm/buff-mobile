# TESTS — Smart Organizer Discovery

## Hat 1 — אוטומטי (רץ, ירוק)

| בדיקה | תוצאה |
|---|---|
| `npx tsc --noEmit` | 0 שגיאות |
| `npx jest src/lib/parentCapture` | 77/77 (7 suites), מתוכם 4 חדשים ל-`entryTelemetry` |
| `npm run i18n:check` | ✅ כל המפתחות הסטטיים נפתרים ב-EN ו-HE |
| `npm run check:no-raw-alert` | ✅ נקי (378 קבצים) |

**מה שה-4 החדשים מקבעים:** חשיפה נספרת **פעם אחת לסשן** ולא פעם לרינדור (הדשבורד מרנדר מחדש בכל refresh ומרכיב מחדש בכל ניווט); משפחה שנייה נספרת בנפרד; ו-`familyId` שעוד לא נטען **לא** מסמן את הסשן כנספר — אחרת החשיפה האמיתית הייתה נבלעת לתמיד.

## Hat 3 — אמולטור (adb)

| # | תרחיש | צפוי |
|---|---|---|
| 1 | דשבורד הורה → לקרוא את הכרטיס | "🗒️ מארגן חכם [בטא] / הדביקי הודעה מאחת הקבוצות — ונסדר ממנה את השבוע" |
| 2 | הקשה על הכרטיס | נוחת **ישירות** במסך הלכידה, לא במרכז "השבוע" |
| 3 | במסך הלכידה — הקשה על "השבוע שלי" | נפתח מרכז "השבוע" |
| 4 | placeholder בשדה ההדבקה | "הדביקי הודעה מאחת הקבוצות…" |
| 5 | החלפת שפה ל-EN וחזרה למסך | "Smart Organizer" / "Paste a message from any of your group chats…" — בלי מפתח גולמי ובלי `{{ }}` |
| 6 | RTL | הכרטיס והקישור מיושרים נכון בעברית |

## Hat 3 — אימות המדידה (DB)

| # | פעולה | צפוי ב-`onboarding_events` |
|---|---|---|
| 7 | פתיחת הדשבורד פעם אחת | שורה אחת `capture_entry_seen`, `source='dashboard'` |
| 8 | pull-to-refresh ×3 + ניווט החוצה וחזרה | **עדיין שורה אחת** — זו הבדיקה שמגנה על כל המדד |
| 9 | הקשה על הכרטיס | `capture_entry_tapped`, ואחריה `capture_opened` |
| 10 | סגירת האפליקציה ופתיחה מחדש | `capture_entry_seen` שנייה (סשן חדש — מכוון) |

## Hat 4 — מכשיר אמיתי (Adi)

- **ויזואלי** — שני המסכים מאחורי אימות, ולכן זה לא ניתן לאימות מהתצוגה המקדימה. לוודא שהתת-כותרת הארוכה לא נחתכת בשתי השפות על מסך צר.
- **ווב (PWA)** — אותה בדיקה בדפדפן; הקוד זהה (JS + i18n בלבד, ללא פיצול פלטפורמה).

## Values Check

- [ ] **Pillar 1** — הכרטיס מתאר יכולת, לא חלון הזדמנות; אין "אל תפספסי", אין מונה, אין דחיפות.
- [ ] **Pillar 2** — הכרטיס לא חוזר ולא מונה ימים שבהם ההורה לא השתמשה; אין ניסוח שמרמז על פיגור.
- [ ] **Pillar 3** — מה שנוצר מגיע לילד כמשימה אמיתית מהחיים שלו; הכרטיס עצמו מסומן להיעלם אם P3 יעלה.

מסמנים אחרי הרצה על ההתנהגות שנבנתה — לא מול הטקסט ב-SPEC.
