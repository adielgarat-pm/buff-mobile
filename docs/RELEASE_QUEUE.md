# BUFF Release Queue

> **The living accumulation surface between releases.** Every fix/feature merged to `main` gets one row here the moment it lands, classified Train or Hotfix. When the train departs, the `buff-release` skill reads the **Queued** rows to seed the release MANIFEST, then they move to **Shipped**.
>
> Policy lives in `docs/RELEASE_PROTOCOL.md`. This file is the data.

**How to use this file**
- **On merge:** add a row to *Queued* with date, PR/commit, type, one-liner, lane, user-facing? and the Flow Suite scenario it maps to (for Gate 2).
- **On cut:** the skill turns every Queued row into a MANIFEST row; after the build is confirmed, move those rows to *Shipped* with the versionCode.
- **Hotfix:** add the row already marked `Hotfix`, cut immediately, then move to *Shipped (hotfix)*.

**Lane** = `Train` (default, batched) or `Hotfix` (bypass — must meet a trigger in RELEASE_PROTOCOL.md).
**Type** = feat / fix / refactor / chore.
**Flow Suite** = the `MASTER_TEST_PLAYBOOK.md` scenario(s) Gate 2 runs for this change. A `feat`/`fix` with no scenario = coverage gap, flag it.

---

## 🚉 Queued — riding the next train

_Last released: **1.7.6 (versionCode 56)** — built 2026-06-23 from commit `1bb0a0b`, web fixes patch. Prior: **1.7.5 (versionCode 53)** commit `9d086e6`. Use `eas build:list` + `app.json` as ground truth — this header is the summary, not the source of truth._

> 📌 **Ground-truth update (CC sync 2026-06-29, `eas build:list`):** since the note above, **vc57** (`c37cc11`) and **vc59** (`57c2f9a`, base `origin/main @ 6710cbb`) both finished on EAS (vc58 errored — lockfile). **vc59 is the latest successful build** and contains the "after 56" table below (#284 / #290 / #285 / #287 / #288 / #289). Whether vc59 is **promoted to a Play track is not visible via CLI — Play Console (Adi) only.** The section directly below is everything merged to `main` AFTER vc59's base → **not in any build yet.**

**Riding the next train — AFTER vc59 (merged post-`6710cbb`, NOT in any build):**

| Date merged | PR / Commit | Type | Change (one-liner) | Lane | User-facing? | Flow Suite |
|---|---|---|---|---|---|---|
| 2026-07-29 | #403 / `b86bbff` | fix | **Smart Organizer put named weekdays one day late.** Found 2026-07-28 running capture end-to-end against prod (run `daa4a942`): anchored on Tuesday 7/28, "the math quiz is on Thursday" came back 7/31 (a Friday) and "pay by Sunday" 8/3 (a Monday); "tomorrow" was correct, so the anchor date itself was fine. Cause: the DATE ANCHORS block passed bare ISO dates, so Gemini had to infer the weekday of a date past its training cutoff — and guessed +1. Weekday arithmetic now happens server-side (`dateAnchors.ts`): today + message date carry their weekday name, plus a 7-row date→weekday lookup covering each weekday's next occurrence, and an explicit "never work out a weekday yourself". Mirrored in the HE branch (with the `ה'` letter forms Israeli class messages use). 14 Jest tests pin the regression. **Server-only — `parse-capture` v12 deployed to prod 2026-07-29 (boot-verified), no app release or OTA needed; live on Android + Web now.** | Server | yes | Capture → paste a message naming a weekday → due date lands on that weekday |
| 2026-07-29 | #404 / `edfe061` | feat | **AI taste gate — non-paying families can finally experience the AI (red team F1).** Both AI gates returned early unless entitled-or-web, but 33 of 35 entitled families hold a lifetime GRANT and only 2 pay → the AI was visible almost only to people who can never convert (measured: non-entitled families who ever saw an insight = **0**). Now **3 free capture runs / family** and **1 free insight / child**, lifetime, native included; the paywall moves to the *next* one. Both gates fail CLOSED on a counter read error. Migration **048 applied to prod** (`child_insights.smart_insight_total_count` lifetime counter + backfill to 1 for the 3 existing insights; `get_smart_insight_state` gains trailing `total_count`, DROP+CREATE in one txn, EXECUTE re-granted). **`generate-child-insights` v17 + `parse-capture` v13 deployed 7/29** (both verified booting). Client half closes two leaks: free tap dead-ending on the Insights lock view (now → Paywall) and a view-log counting a card that never rendered. **Server + DB already live; the client half needs an OTA.** | OTA | yes | Free (non-entitled) Android parent → dashboard shows a real AI insight + "next one locked" upsell → tap → Paywall; 4th capture run → paywall |
| 2026-07-29 | #402 / `6b2364e` | feat | **AI insight usage made measurable (red team F4).** `smart_insight_feedback` shipped 5 outcome columns and every one was 100% NULL. Four are DERIVABLE from existing state (`tasks`/`store_rewards`/`stickers`/`daily_progress`) → derived in `scripts/insight-usage.sql` instead, retroactive over every insight ever generated, zero client risk. Only the two underivable signals became events: `insight_viewed` / `insight_cta_clicked` in `onboarding_events` (source = `dashboard` \| `insights_screen`). CTA logged at the smart-insight call site, not inside the shared `runCta`. View dedup keyed on (child, placement) after prod wrote two rows 66ms apart from a flapping `computedAt`. No schema change, no UI change. Red-team doc added at `docs/research/RED_TEAM_AI_FEATURES_2026-07-28.md`. **Client-only → needs an OTA to start reporting.** | OTA | no | Dashboard renders AI card → `onboarding_events` gets `insight_viewed`; tap CTA → `insight_cta_clicked` |
| 2026-07-28 | #398 / `fa57853` | fix | **Smart Organizer stuck-spinner fix — two-sided parse timeout.** Root cause of Adi's 7/28 "broken after the update" report: a real Gemini parse takes 60-90s with no timeout/progress copy on either side (server data showed both runs SUCCEEDED). Client: 120s `withTimeout` backstop + abort signal + `capture.workingHint` while parsing + typed timeout error string. Server: `parse-capture` **v11 deployed 7/28** (100s `AbortSignal.timeout` on the Gemini fetch → typed 504 + `error_gemini` row, `gemini_ms` logged). Ordering 100s < 120s, both above measured latency. **Production OTA dispatched same day (run 30375970735).** | OTA | yes | Capture → paste → spinner shows "up to a minute" hint → items; dead connection → timeout copy (not endless spinner) |
| _open_ | #394 / `pkg/capture-usage-metrics` | feat | **Capture usability metric** — migration 047 (additive: `capture_runs.outcome`/`kept_count`/`discarded_count`/`edited_count`/`confirmed_at`, `parent_items.capture_run_id`) **applied to prod 7/27**; `parse-capture` **v10 deployed** (logs failed runs, returns `run_id`, daily cap counts only billable runs); client sends the confirm summary + `capture_opened`/`capture_consent_granted` to `onboarding_events`; report in `scripts/capture-usage.sql` (North Star = Repeat Capture Rate 7d). No UI change. **Server + DB are already live; the client half needs an OTA to start reporting.** | OTA | no | Capture → paste → confirm → `capture_runs` row has kept/discarded/edited + `confirmed_at` |
| 2026-07-22 | #388 / `56c3adb` | fix | **Capture/Timetable Android file read fixed** — SDK 54 moved `readAsStringAsync` to `expo-file-system/legacy`; root import throws at runtime, so every Android file pick failed client-side (day-1 beta report by Adi). Import switched in `parseCapture.ts` + `TimetableScreen.tsx`. Hat-3 e2e on emulator: Hebrew camp-schedule image → 12 items. Follow-up guards (check script, Sentry, WORKFLOW rule 12) in `pkg/capture-guards`. **Needs production OTA dispatch.** | OTA | yes | Capture → Add a file (image/PDF) → Read it → items; Timetable → photo import |
| 2026-07-22 | #386 / `f68ea5e` | fix | **`daily_progress.completed_at` written on completion again** — re-land of `982a258` that was stranded on unmerged `pkg/ai-trial-and-referral`; 286 NULL rows backfilled in prod (data-only). Also stops useKidLocalNotifications reminding about an already-done first task. **Production OTA dispatched same day (run 29942159068).** | OTA | yes (wrong-reminder fix) | Child completes task → daily_progress row has completed_at; first-task local reminder not sent after completion |
| 2026-07-22 | #385 / `0e495e5` | feat | **Own-device child language beats name-script guess** (rescue of #384, which merged into #383's closed base and never reached main). `resolveChildLang` ownDevice + `language_source` (parent/inferred/device) + starter-task retitle on language change (exact library matches only) + `preferred_language` kept in sync. Gur (Noa Ganot family) data-fixed to Hebrew server-side 7/21 (live on current binary). | OTA | yes | Child own device (Hebrew locale, Latin name) → UI Hebrew + starter tasks retitled; EditChild language toggle → locks choice + retitles |
| 2026-07-22 | #383 / `bdf82f7` | feat | **Teens start in Gamer theme** — onboarding seeds `pro_settings.child_theme` ('gamer' for 12-14/15-18), ThemeContext falls back to DB when AsyncStorage empty (reinstall/new-device too); 11 existing teen profiles backfilled to gamer in prod (incl. Gur). | OTA | yes | Create 15-18 child → child session opens in Gamer (wolf); existing teen w/o chosen theme → Gamer after OTA |
| 2026-07-21 | #380 / `936efd9` | feat | **Parent Capture BETA ON** — paste text/file → Gemini → tasks. parse-capture hardened to insights posture (auth + entitlement + 30/day cap), EN prompt path, beta disclaimer HE/EN, web file-read fix. Edge Fn v8 live. **Production OTA published same day (runtime `e4983c15` = vc68 ✅).** | OTA | yes | Parent dashboard → This Week → Capture → paste → items → confirm → child task |
| 2026-06-27 | #295 / `2abbc02` | fix | **Pin Supabase `storageKey` — recover sessions lost by the `api.buffadhd.com` (#290) switch.** The custom-domain switch derived the storage key from the URL host, mass-logging-out existing users; this pins it to the old value. ⚠️ Intended to ride the SAME build as #290 — but #290 is in **vc59** and #295 merged after → **#295 is NOT in vc59.** | Train | yes | Sign in → kill/relaunch app → session persists (not logged out) |
| 2026-06-27 | #296 / `e1c1d78` | fix | **Web onboarding no longer resets to step 1** + UStep4 footer reachable on web PWA. | Train | yes (web) | Onboarding (web) → advance → does not snap to step 1 |
| 2026-06-27 | #297 / `404cee6` | feat | **Web onboarding reload-safe** — web-only nav-state persistence so a reload resumes the same step. | Train | yes (web) | Onboarding (web) → reload → resumes step |
| 2026-06-28 | #298 / `6cd7087` | fix | **RTL on UStep1-4/7/8** + fail-safe task/reward save in onboarding. | Train | yes | Onboarding (Hebrew/RTL); task + reward save |
| 2026-06-28 | #299 / `c819108` | fix | **Low-risk a11y + input polish** on UStep1/2/3/4/8. | Train | yes | Onboarding input / a11y |
| 2026-06-28 | #300 / `c1050b8` | test | **E2E suites (web + Android) + GitHub Actions CI.** Test/CI infra; no app-runtime content. | Train | no | CI only |
| _open_ | #301 / `pkg/smart-join-link` | feat | **Device-aware HTTPS child-invite link** (`buffadhd.com/join/CODE`) replacing the dead `buff://` link — Android App Link + Play Install Referrer auto-fill; targets the NOT-STARTED funnel leak. **PR OPEN (draft) — adds on merge.** | Train | yes | Child invite link → device routing → ChildJoin pre-filled (Hat-4: App Link + referrer on a real Play install) |
| _open_ | #317 / `pkg/web-ai-coach-free` | feat | **AI coach free on web; trial gate stays on Android.** Web is unconditionally unlocked for Smart Insights (no 14-day trial, no paywall) while the web build matures; Android/iOS unchanged. Client: `insightsUnlocked` + auto-gen guard + invoke sends `platform`; server `generate-child-insights` **v13 (already deployed)** skips the 402 when `platform==='web'`, still capped by 3/child/week. **Web lands on Vercel redeploy after merge; native behavior unchanged so no build gating.** Intentional platform divergence — DECISIONS_LOG entry pending Adi. | Train | yes (web) | Web (logged-in parent) → Insights → coach generates, no paywall · Android free user → still gated to Paywall |

> 🚧 **CUT — 1.7.7 (versionCode 63), 2026-07-03.** Production AAB = EAS build `c8dc323b-02cb-4f08-9c13-02fd581f56c2` from `origin/main @ 80865e7` (version 1.7.6→1.7.7). **Content = everything merged after the vc59 base (`6710cbb`):** the rows above — #295 storageKey session-recovery · #296/#297/#298/#299 web onboarding (reset/reload-safe/RTL/a11y) · #300 E2E+CI · #303 web Google-OAuth token consume · #304 web-entry deep-link + responsive auth · #306 referral share-to-all-apps + click tracking · #307 admin exclude-Eli · #308 billing (rc-webhook env-gate + Founding one-time fix + close beta-grant window) · #310 rate-us unified rating screen — **plus #313 AI-trial client surface** (gate + "🔒 unlock coach" teaser + "✨ AI coaching active" trial ribbon + lazy auto-generate). **AI-trial scope note:** only Phases D/E shipped; the web-to-native-CTA package + Phase C referral bonus that #309 squash-merged alongside were deliberately EXCLUDED (still on `pkg/ai-trial-cta-polish`). Server side (0/A/B — entitlement guard 035, token gate 036, trial clock 037, edge-fn `generate-child-insights` v12) was already live on the DB. **Gate 1** ✅ tsc 0 · jest 505/505 (ThemeContext suite = pre-existing Node/WS env fail). **Gate 2 → Hat-4:** S2 (free user gated → locked teaser → Paywall) verified on a preview build + real device 2026-07-03 (screenshot-confirmed); purchase not testable on preview ("not configured for Play billing" — expected). Rows move to **Shipped (63)** once Adi promotes + "verified, tag it". **#301 (draft) NOT included.** Follow-ups surfaced during Hat-4 (native notif-prompt re-pop loop; teen age-fit of insight/coach content) → `docs/sessions/ai-trial-and-referral/FOLLOWUP_PROMPT.md`.
>
> ⚠️ **versionCode history for this cut (why 63, not 60):** **vc60** (`b6aad996`, `113f3e2`, 1.7.7) was the first clean production build but Play routed it to the **Alpha** track. **vc61** (`93c7babf`, `732be98`, **1.7.6**) was then built **from the stale `pkg/ai-trial-cta-polish` branch** — ~2 weeks behind main, missing #295/#303/#308/#310 and regressive — and reached **Production + Open testing** (rollout halted). Since Play requires a strictly-higher versionCode per track, the fix was a fresh build from `origin/main`: **vc62** (`151ce1c8`) was accidentally started from the wrong branch again and **canceled** (versionCode burned); **vc63** (`c8dc323b`, `80865e7`) is the correct clean build and the one to promote. **Lesson:** build the release **only** from `origin/main`, and verify `HEAD` + `app.json version` **before** every `eas build` — a `git checkout` that aborts silently leaves you on the previous branch.

> 🚉 **NEXT BUILD (vc64) — merged to `main` AFTER vc63's base (`80865e7`), NOT in any build yet.** No urgency: nothing here is a regression, and the AI-coach value is already live server-side/web (see the ✅ line). Cut vc64 from `origin/main` only after vc63 clears Google review — bundle these together.
>
> | Date merged | PR / Commit | Type | Change (native-relevant) | Lane | User-facing? | Flow Suite |
> |---|---|---|---|---|---|---|
> | 2026-07-03 | #316 / `bbf84a9` | feat | **Android mobile-web install CTA** (web-to-native) — Phase 1+2 + dashboard nudge; convert Android mobile-web signups to the native app. | Train | yes | Android mobile-web → "Get the app" CTA → Play install |
> | 2026-07-04 | #317 / `ff5efd9` | fix | **Per-child insight reset** — `useSmartInsights` now clears state on child switch, so one child's AI insight can't linger on another (Itay→Emi). Cosmetic, within-family; low severity. **The rest of #317 is NOT a build item:** web-free + `platform` send are web-only / no-op on native; the Hebrew coach prompt **v14** + feedback loop are **server-side and already live**. | Train | yes (paid/trial, multi-child) | Insights → switch child selector to a child with no insight → shows empty/own, never the other kid's |
> | 2026-07-06 | #319 / `pkg/fix-pause-visibility-child` | fix | **Paused tasks now actually disappear for the child** (real-user report, Noa). Mint tasks+shop pause gates (Gamer had them, Mint didn't) + `tasks` added to the realtime publication (migration 040, **already applied to DB**) + live child refetch on task edits + AppState-foreground fallback + one-time-task edit no longer shows a lying "paused" hint. | Train | yes | Family pause → Mint child tasks/shop show break state; parent sets task days=[] → open child session drops it live (Hat-4: two devices) |
> | 2026-07-06 | #320 / `pkg/timetable-editor-overflow` | fix | **Timetable editor save footer always tappable** (real-user blocker, Noa) — safe-area bottom padding on manual/review/paste footers + KAV + bounded multiline equipment field. **Includes the header fix (committed in the #322 stack): the screen's header rendered UNDER the status bar on edge-to-edge devices — "Update Schedule" was untappable (reproduced+fixed+re-verified on emulator).** | Train | yes | Schedule → manual entry with long equipment → save button reachable; header "Update Schedule" tappable |
> | 2026-07-06 | #321 / `pkg/timetable-time-picker` | feat | **Native OS time picker for lesson times** (web: `<input type="time">` via web-split TimeField) replacing free-text HH:MM in the timetable editor. | Train | yes | Schedule → manual entry → tap lesson time → Material clock opens → value persists |
> | 2026-07-06 | #322 / `pkg/timetable-copy-day` | feat | **Copy a day's lessons (incl. equipment) to other days** — multi-day picker + explicit add/replace confirm, never silent overwrite. Manual mode v1. | Train | yes | Schedule → manual entry → copy day → target day shows merged lessons → child Gear tab shows copied equipment |
> | 2026-07-07 | #323 / `pkg/timetable-time-picker` (rider) | fix | **Schedule can finally be cleared / replaced** (Adi, season change school→camp): "🗑 Clear this day" button + saving an EMPTY schedule allowed behind an explicit destructive confirm (was double-hard-blocked — disabled button + early return). | Train | yes | Schedule → manual → clear day → save empty (confirm) → view shows empty state; camp replace: clear → enter → copy-day → save |
>
> ✅ **Already LIVE without this build (do NOT re-cut for these):** AI coach **Hebrew prompt v14 + feedback loop** (edge fn `generate-child-insights`, live for all platforms) · **web AI-coach free** (Vercel) · **web per-child fix** (Vercel). Only the two rows above actually need vc64.
>
> 🚧 **CUT — 1.7.8 (versionCode 64), 2026-07-07.** EAS build `7bf02b27-f54e-4e17-8ca7-aa0c8edb0b5a` (production app-bundle) from `pkg/release-64 @ 6f320a5` (= `origin/main @ b0a3945` + version bump 1.7.7→1.7.8 only — base verified: HEAD, content greps for all 6 features, tsc 0). **Content = ALL rows in this vc64 table** (#316 · #317 · #319 · #320 · #321 · #322 · #323-rider). Manifest: `docs/releases/64/MANIFEST.md`. Gate 1 ✅ tsc 0 · 571/571 jest (#323 CI green pre-merge). Gate 2 ✅ Hat-3 emulator LIVE incl. a full camp-schedule E2E (clear → time-picker → copy-week REPLACE → save → DB-verified). Gate 2 → Hat-4 (Adi, real device): two-device pause propagation; timetable long-equipment save + header tap; known friction: denied-notifications banner overlaps bottom CTAs (fix chip open — NOT gating). Rows move to **Shipped (64)** once Adi uploads + promotes + "verified, tag it". Migration 040 already applied — nothing DB-gated to this build. vc63 (1.7.7) was never promoted; vc64 supersedes it.

> 🌐 **Not in the AAB (deploy via Vercel, not a build):** #294 landing redesign (already LIVE on buffadhd.com 2026-06-27) · #292 / #293 download-page + launch docs · `f71d97f` lockfile fix (its content already in vc59).

**Riding the next train (after 1.7.6 / versionCode 56):**  ← _per `eas build:list` this content shipped in vc59; move to Shipped at next cut_

| Date merged | PR / Commit | Type | Change (one-liner) | Lane | User-facing? | Flow Suite |
|---|---|---|---|---|---|---|
| 2026-06-26 | #285 / `pkg/fix-web-push-prompt-loop` | fix | **Web push permission pre-prompt no longer re-pops in a loop.** Persists the dismissal in AsyncStorage (web localStorage + native — platform parity) with a 7-day re-ask window, replacing a reload-fragile `useRef`; `handleAccept` marks dismissed before `register()` so a failed web accept (permission stays `'unknown'`) can't re-show the modal. Android unchanged; no schema, no deps. | Train | yes (web) | Sign in (web, parent) → dismiss push pre-prompt → reload ×N → does not reappear |
| 2026-06-26 | #284 / `pkg/rate-us-port` | feat | **In-app Rate BUFF + feedback flow.** Native (Android/iOS): OS in-app review card (`expo-store-review`) fires automatically on the parent dashboard once the retention gate passes — no sentiment question / no CTA button (Play + App Store policy); suppressed in view-as-child. Web: banner + sheet → first-party `reviews` table. Settings: **Rate BUFF** (native → Play deep-link; web → sheet) + **Send feedback** (un-gated private channel) rows; kept the existing referral row. Admin: **Rate Feedback** tab (FeedbackBoard) w/ parent email + family link. **New native dep `expo-store-review` → cannot OTA; ships only on a fresh build.** | Train | yes | **NEW SUITE NEEDED — rate/feedback.** Gate-2 = Hat-4 on a real EAS build: (1) launch safety w/ the new native import, (2) native review card fires on dashboard, (3) Settings Rate → Play listing |
| 2026-06-26 | #290 / `pkg/supabase-custom-domain` | fix | **Supabase API now served from `api.buffadhd.com` (Custom Domain) to bypass the Israeli-carrier `*.supabase.co` TLS/SNI reset.** Real testers hit `ERR_CONNECTION_RESET` on first load over some IL cellular networks (WiFi worked) — carrier SNI-filters the `supabase.co` hostname, resetting the TLS handshake before it reaches the (healthy) backend. Activated the Supabase **Custom Domain** add-on (Pro) and pointed `EXPO_PUBLIC_SUPABASE_URL` → `https://api.buffadhd.com` (CNAME → `gfrongfnyigxsexuofrg.supabase.co`, Google Trust Services cert) across all 3 EAS profiles + local `.env`. The original `*.supabase.co` domain **stays live** (Supabase keeps both) → existing installs unaffected. Verified end-to-end (HTTP 200 + cert `CN=api.buffadhd.com`). **Config-only; no schema, no deps. Changes a build-time env → NOT OTA-able; ships only on a fresh build.** Platform parity: Android (eas.json) + Web (`.env` / redeploy). ⚠️ Proof on an actually-blocked IL carrier still pending (Maya unresponsive) — recommend opening `https://api.buffadhd.com/auth/v1/health` on the failing cellular before relying on it. | Train | yes (IL cellular) | NEW SUITE NEEDED — first-load reachability: on a blocked IL carrier (cellular, WiFi off) the app reaches the backend via `api.buffadhd.com` where `supabase.co` was reset |
| 2026-06-26 | #287 / `51b6133` | fix | **Web-split birthday field so the onboarding date picker works on web PWA.** The birthday step used a native-only `DateTimePicker`, leaving a dead control on web (suspected cause of a stalled web tester). Platform-split `BirthdayField` (`.tsx` native / `.web.tsx` `<input type="date">`). Android unchanged; no schema, no deps. | Train | yes (web) | Onboarding → birthday step (web) |
| 2026-06-26 | #288 / `6a2c922` | fix | **crossAlert shim — `Alert.alert` dialogs now work on web.** RN `Alert.alert` is a no-op on Expo Web; added `src/platform/crossAlert` (native passthrough / web `window.confirm`-style) + `npm run check:no-raw-alert` guard. Android behavior identical; no schema, no deps. | Train | yes (web) | Any confirm/alert dialog (web) |
| 2026-06-26 | #289 / `5f23790` | fix | **Rename `InstallNudge.ts` → `.tsx` so the PWA install banner renders on web.** The nudge component carried JSX in a `.ts` file; Metro web resolution skipped it. Extension-only fix; no logic change, no schema, no deps. | Train | yes (web) | Parent Dashboard → PWA install banner (web) |
| 2026-07-01 | #306 / `pkg/referral-to-main` | feat | **Referral: share to all messaging apps + fix dead join link + click tracking + admin funnel.** Share now uses the native OS/`navigator.share` sheet (all apps) with `wa.me` desktop fallback (was WhatsApp-only). New landing `/join` route carries `?ref=` into the app + app captures it at module load, so web auto-redeem finally fires (was the cause of 0 real redemptions). Click tracking via new `referral_clicks` table + public `track-referral-click` Edge Function (crawler-filtered) + `admin_referral_funnel()` RPC + 🎁 Referrals admin tab. **Migrations `033`/`034` + Edge Function already applied to prod via MCP** (no gating needed at cut). **Web: needs Vercel redeploy of BOTH `buff-landing` (for `/join`) and app/www (for capture); native share change rides the next build.** No new deps. | Train | yes | NEW SUITE — referral: (1) invite sheet lists multiple messaging apps; (2) click `buffadhd.com/join?ref=X` → lands in app → complete onboarding → both families get 14d premium + click shows in 🎁 Referrals; (3) copy-link + manual-code entry still work |

> 🚧 **CUT IN PROGRESS — 1.7.6 (versionCode 59):** EAS build `2fdce208-8cb6-4e62-a205-235255411f2b` (production app-bundle) building 2026-06-26 from `pkg/release-58 @ 57c2f9a` (= `origin/main @ 6710cbb` — incl. #290 + #291 — + lockfile fix `57c2f9a`). versionCode 59 via `autoIncrement`. ⚠️ **versionCode 58 was burned**: the first attempt (build `ce60ad21`) failed at EAS "Install dependencies" because #284 added `expo-store-review` to package.json but never regenerated `package-lock.json` → `npm ci` mismatch. Fixed by `57c2f9a` (`npm install --package-lock-only`). **The lockfile fix must also reach `main` via PR, or the next build from main repeats the failure.** Draining into `docs/releases/58/MANIFEST.md`. **Headline content (needs this build — not OTA-able):** #284 in-app review + feedback + admin FeedbackBoard (`expo-store-review` native dep) · #290 `api.buffadhd.com` custom domain (build-time env, the IL-carrier reset fix). **Web-parity (ride along; also need a Vercel redeploy w/ new env):** #285 push-prompt-loop · #287 web birthday field · #288 crossAlert · #289 InstallNudge `.tsx`. **Gate 1:** tsc = 1 pre-existing web-push type error (web-only, build-safe, not in this train's headline); jest 493/494 (1 env-harness fail `ThemeContext` + i18n guard flags 5 **pre-existing** files — Insights #264 / Smart-Capture #276 / Landing #281 — NOT #284/#290). **Gate 2 → Hat-4 device (Adi):** (1) launch safety w/ new native import, (2) #284 OS review card fires on dashboard + Settings Rate → Play, (3) #290 first-load on a blocked IL carrier (`api.buffadhd.com/auth/v1/health`, WiFi off). Rows move to **Shipped (58)** once Adi promotes + "verified, tag it". No new migration gated to this build; no Edge Function gated. ⚠️ **Web still needs a Vercel redeploy** to deliver #290 + the web-parity fixes to the PWA. **i18n debt (5 files) = separate fast-follow, not blocking.**

> ✅ **Shipped — 1.7.1 (versionCode 52), 2026-06-22.** Hotfix: View-as-Child reads child's theme from DB (`e70fa92`). Rows that rode the 1.7.0 train are in Shipped below.

> ✅ **Shipped — 1.7.0 (versionCode 51).** Content: #271 web-responsive-shell · #272 web-push-pwa · #273 task-day-selector-empty-reopen · web-push VAPID · AI insights Phase 1-2 · #258 weekday selector forward-port · #260 reward edit/duplicate · #261 dev-client splash fix · #264 Rich Parent Insights · #252 Recommended-now card · #267 PWA install nudge · #276 Smart Capture · #277 referral program. Rows move to Shipped below once confirmed.

> ✅ **Shipped — 1.6.1 (versionCode 48), 2026-06-16.** EAS build `92e07549`. Content: #253 completion confetti+sound · #252 "Recommended now" parent card + `89ab9cc` realtime crash fix · #244 BUDDY gift loop + L4/L5 · #248 reward size-null label fix · #243 off-routine leak fix · #251 expo-web hide native import.

> 🚧 **CUT IN PROGRESS — 1.6.1 (versionCode 48):** [SUPERSEDED — see ✅ above] EAS build `92e07549-2d0d-4e1a-93eb-ba4ca21a9ba0` (production app-bundle) queued 2026-06-16 from `pkg/release-48 @ 837d11b` (= `origin/main @ 2b47941` + prep). Draining into `docs/releases/48/MANIFEST.md`. **Content = the 11 merges since the vc47 cut (`bc38628`):** app-facing → #253 completion confetti+sound · #252 "Recommended now" parent card + `89ab9cc` realtime crash fix · #244 BUDDY gift loop + L4/L5 · #248 reward size-null label fix · #243 off-routine leak fix · #251 expo-web hide native import. Web-landing/docs only (NOT in build) → #245/#246/#249/#247/#250. Gate 1 ✅ (tsc 0 / jest 423/423; fixed a #253 expo-audio jest-mock gap). Gate 2 → Hat-3 emulator + Hat-4 device pending. Rows move to **Shipped (48)** once Adi promotes + "verified, tag it". No new migration; no Edge Function gated to this build.

> ✅ **Cut closed — 1.4.0 (versionCode 34), promoted to Alpha 2026-06-08.** The rows that were riding the train shipped (the fixes via 1.3.1(31); the features stacked on top via 1.4.0(34)) and have moved to **Shipped** below. versionCode **32 and 33 were burned** during the 2026-06-08 cut — 32 consumed remotely, 33 a parallel/superseded build that was never promoted; **34 is the successful, promoted build**. **Next train starts clean: the next production build will be versionCode 35** (EAS `appVersionSource: remote` + `autoIncrement: true` tracks the last code server-side); bump `versionName` at cut time (e.g. 1.4.1 / 1.5.0) to avoid a Play collision with the already-uploaded 1.4.0.

> 🚧 **CUT IN PROGRESS — 1.4.1 (versionCode 39):** all rows below are draining into `docs/releases/39/MANIFEST.md`. EAS build `37f1c9ee` building 2026-06-08 from `main @ 90956ed` (Gate 1 ✅). They move to **Shipped** once Adi promotes 39 to Alpha + says "verified, tag it". (Build 38 was cut then **canceled** — superseded by 39, which adds the notifications UI; codes 35/36/37 burned by parallel builds.) ⚠️ Deploy the `push-notification-fanout` Edge Function only when 39 is promoted.

**Riding the next train → versionCode 39:** these merged to `main` *after* the 34 cut point (`4139d2e`, 2026-06-08 10:48) and are therefore **NOT in the live Alpha build 34** — they ship in 39.

| Date merged | PR / Commit | Type | Change (one-liner) | Lane | User-facing? | Flow Suite |
|---|---|---|---|---|---|---|
| 2026-06-23 | #278 / `04aadaf` | fix | **Onboarding step 7 "No phone" button now works on web PWA.** React Native `Alert.alert()` doesn't work reliably on Expo Web, causing the 👨‍👩‍👧 button to be unresponsive. Replaced with direct navigation `goNext(false)`, matching user intent. One-line fix, no schema, no deps. Android unaffected. | Train | yes | Onboarding → UStep7 → "No — show on my device" |
| 2026-06-08 | #198 / `ba5ca8a` | fix | **Pause Mode ends at local midnight (calendar day)** instead of rolling N×24h from tap time. The exact bug רחל + Adi hit live on war-day 2026-06-08 (only their 2 DB rows were hand-fixed; everyone else on build 34 still has the +24h behavior). Code-only, merge-verified, Hat-3 logic verified; Hat-4 device pending | Train | yes | Parent Settings → Pause |
| 2026-06-08 | #199 / `d449997` | feat | **Off-Routine Day** ("hard-day mode") — per-child third day-state; swaps the weekday plan for a light age-banded anchor bank, app stays active, still earns BUFFs. Pause supersedes off-routine. Hat-3 robustness done; Hat-4 pending | Train | yes | EditChild → Off-Routine card |
| 2026-06-08 | #201 / `efd5569` | fix | Off-Routine "3 days" ends at local end-of-day (today+2), consistent with the Pause calendar-day fix | Train | yes | EditChild → Off-Routine "3 days" |
| 2026-06-08 | #194 / `35d4c9d` | fix | Remove the Dev-Simulate-Subscribed toggle from parent settings (was a dev-only control visible in prod) | Train | yes | Parent Settings |
| 2026-06-08 | `pkg/notifications-client` | feat | **Notifications UI (Phase 4) + Edge enforcement (3b)** — the permission model that finally makes the live server nudges reach testers (was: 0 device tokens registered). Two-toggle Settings screen ("Alerts to me" / "Reminders for my child"), denial-recovery banner (deep-link to system settings — fixes "denied → dark forever"), Edge Function suppresses per family pref (`pref_off`). ⚠️ **Deploy the Edge Function only when this build is promoted** (kid reminders default off). | Train | yes | Parent Settings → Notifications; NotificationGate denial banner |

> 🚧 **CUT IN PROGRESS — 1.4.3 (versionCode 41):** EAS build `0bbd6332` building 2026-06-10 from `pkg/release-41 @ 1148f84` (origin/main `07ba6d0`). Gate 1 ✅; Gate 2 → Hat-4 (delta = banner fix + #211, both real-device). This = **build 40 content + the two rows below** (`80782b4` banner fix + `f6fbc14` #211), which build 40 (1.4.2) predated. They move to **Shipped (41)** once Adi promotes 41 + "verified, tag it". ⚠️ Deploy `push-notification-fanout` Edge Function only when 41 is promoted. (`#209 cf6b353` already shipped in build 40.)

> 🚧 **CUT IN PROGRESS — 1.4.4 (versionCode 42):** EAS build `0e62b1b4` building 2026-06-11 from `pkg/release-42 @ 892c564` (origin/main `0034200`). **Supersedes build 41** (1.4.3, never promoted) — 42 = all of 41's content + the three 2026-06-11 merges (#219 platform backfill, #220 rewards focus-refetch, #221 safe-area top). Gate 1 ✅ · Gate 2 ✅ Hat-3 emulator (all three delta items live-verified — see `docs/releases/42/MANIFEST.md`). Rows move to **Shipped (42)** once Adi promotes + "verified, tag it". ⚠️ Deploy `push-notification-fanout` Edge Function only when 42 is promoted. (PR #222 — the standalone queue row for #220 — is folded into this update.)

**Riding the train AFTER 39 (merged post-cut `90956ed` — NOT in build 39):**

| Date merged | PR / Commit | Type | Change (one-liner) | Lane | User-facing? | Flow Suite |
|---|---|---|---|---|---|---|
| 2026-06-10 | `fix/notif-banner-safe-area` / `80782b4` | fix | **Denial-permission banner safe-area** — the "notifications off → enable in settings" banner (shipped in build 39 / 1.4.1) used a fixed `bottom:24` and collided with the Android system nav bar, making its CTA + dismiss untappable. Now sits above the parent tab bar + system nav via `useSafeAreaInsets`. Layout-only. Reported live on 1.4.1. | Train | yes | NotificationGate denial banner |
| 2026-06-09 | #209 / `cf6b353` | feat | **Reward-redemption request discovery + "let's talk" reset.** Notification tap → Rewards tab + auto-selects the requesting child; per-child tab dot for open requests; focus refetch (the engine was correct — this fixes *reachability*: parent couldn't get from notification to the approve button). "Let's talk" is now a two-sided reset: leaves the parent's list; child sees "parent wants to talk" + "Got it" → clears; child re-requests after the talk (new status `discussed`; migration 025 **already live on DB**). tsc+jest+DB verified; **on-device Hat-3 pending**. | Train | yes | Child Rewards → Redeem (request); Parent Rewards → Approve / Let's talk; Notification → tap → child select |
| 2026-06-10 | #211 / `f6fbc14` | feat | **Kid shares a good mood with a parent** — a non-low Vibe Check (level ≥3) offers an in-flow "share this feeling?" step; on opt-in the parent gets a gentle push (gated on the existing **"Alerts to me"** toggle) **+ an INFO bell row**. Positive, kid-initiated twin of SOS; private by default. migration `025_vibe_shared_notification` (`child_vibes.vibe_shared_with_parent` + `handle_vibe_shared` trigger) **already live on DB**. ⚠️ **`push-notification-fanout` Edge Function NOT yet deployed** → push stays silent until deployed (bell row works once a build ships). Copy is **draft** (kid + push — Adi/Itay gate). tsc + jest(28) + trigger-probe verified; **on-device Hat-3 pending** (emulator contention). File number 025 collides with #209 — harmless (distinct DB versions). | Train | yes | Child Vibe Check → share step; Parent notification feed |
| 2026-06-11 | #221 / `pkg/safe-area-top` | fix | **Top system bar overlapped tappable controls on 17 screens** — RN-core `SafeAreaView` is a no-op on Android while the app runs edge-to-edge, so custom top bars rendered under the status bar (reported live: "Mark all as read" in the notification feed unreachable). Swapped to `react-native-safe-area-context` + `edges={['top']}` on all 17 affected screens (feed, paywall, onboarding ×9, auth ×2, edit/manage-child, philosophy, founding-hundred). Hat-3 emulator-verified: top bar starts at the 136px inset, button tap registers + DB `is_read` flips. | Train | yes | Notification feed → Mark all as read; Onboarding flow |
| 2026-06-10 | #216 / `96dee51` | fix | **Off-Routine banner/card follow the interface language** — `OffRoutineBanner` + `OffRoutineCard` chose copy via `language === 'he' ? …` off `useLanguage()` (device language), which i18next/`ModeContext` deliberately doesn't sync in View-as-Child → the purple "day off" banner stayed Hebrew inside Leia's otherwise-English preview (Adi caught it live on the off-routine day she set). Strings moved to `t()` + en/he.json (**copy byte-identical**); adds a zero-dep Jest guard (`i18nNoHardcodedCopy`) so the pattern can't regress (3rd time of the IN-2026-05-27-04 class). tsc clean; jest env broken on machine → guard verified via raw node; **on-device Hat-3 pending**. | Train | yes | View-as-Child → Off-Routine banner; EditChild → Off-Routine card |
| 2026-06-11 | #220 / `fad5ed2` | fix | **Child Rewards tab no longer shows a stale balance + "pending" badge after parent approval** — HQ refetched on focus but the Rewards tab (Mint + Gamer) fetched only on mount, so after approval the two screens disagreed. Both screens now refetch balance + open redemption requests on every focus. Hat-3 verified live 2026-06-11 (emulator): DB balance change + DB approval both reflected on simple tab refocus, badge cleared | Train | yes | Child Rewards → Redeem → parent approve → child Rewards refocus |
| 2026-06-11 | #219 / `aaf1dda` | feat | **families.platform backfill on app-open + platform in admin Tester Board** — migration 026 RPC (idempotent, fills only NULL) called once per profile load; drains the 203-family NULL cohort as testers open this build; admin-web PlatformBadge. Hat-3 verified live: test family flipped NULL→android on app open | Train | no (analytics) | Admin Tester Board (web); silent app-open call |
| 2026-06-12 | `pkg/ios-testflight` | feat | **iOS readiness bundle**: (a) **in-app account deletion** (Apple 5.1.1(v) + Google Play requirement) — Settings Danger-Zone row → `delete_my_account` RPC (migration 021, **already live on DB**); sole parent deletes family, co-parent/child deletes self only. (b) **Sign in with Apple** (Guideline 4.8) — native button on Login/Signup, **iOS-only, renders null on Android** (verified: import-safe without dev-client rebuild via `requireOptionalNativeModule`). (c) iOS config: alpha-free icon, `usesAppleSignIn`, encryption-exempt flag. On Android only (a) is visible. tsc clean; web preview verified; account-deletion device-verified 2026-06-11 | Train | yes (deletion row) | Parent Settings → Danger Zone → Delete account |
| 2026-06-13 | `pkg/fix-streak-counter` | fix | **Day-streak stuck at 0** (Alon's bug). The streak increment (`onTaskCompleted`) existed but was wired to nothing; `useChildData.completeTask` now advances it via `applyTaskCompletionToPet` on every real incomplete→complete transition (idempotent per day). Also unblocks Buddy evolution (`evolution_days_count` was frozen by the same disconnect). Dashboards `reload()` pet state on focus. tsc clean; 41 existing + 4 new streak tests pass; **on-device Hat-3 pending**. 🚩 streak still per-device (AsyncStorage) — per-child Supabase is fix B (next pkg). | Train | yes | Child HQ / Quests → complete task → streak badge |
| 2026-06-13 | `pkg/streak-per-child` | fix | **Streak is now per-child, not per-device** (fix B — supersedes the FLAG on the fix A row above; on shared devices siblings no longer share one streak). Derived on read from `daily_progress` via new `child_task_streak(p_child_id)` RPC (migration `029`, **already applied to DB**) — consecutive days ending today/yesterday with ≥1 completed task; no stored counter (can't drift), no backfill (every existing tester gets their real streak at once — Alon included). New `useChildStreak` hook; both dashboards read it instead of `pet_state.daily_streak`. DB-probed live (active child=4, broken=0, gap-split correct); tsc clean (− pre-existing apple-auth); 28 tests pass. **on-device Hat-3 pending**. 🚩 residual: Buddy evolution/XP still per-device. | Train | yes | Child HQ / Quests → complete task → streak badge (per child) |
| 2026-06-14 | #236 / `27a4495` | fix | **Onboarding "I'll do it later" now schedules a real 24h reminder.** UStep7 Option B ("Yes — I'll do it later" → "we'll remind you in 24 hours") was a silent no-op — it only advanced the step, never scheduled anything (a stale comment wrongly claimed expo-notifications wasn't installed; it is, `~0.32.17`). Now requests notification permission + schedules a local `TIME_INTERVAL` notification 24h out, reusing the `'default'` Android channel; best-effort, so a denied permission or scheduling error never traps the parent in onboarding. No new deps, no schema. Emulator-verified end-to-end (dumpsys confirmed a ~24.0h RTC_WAKEUP alarm) + tsc clean. | Train | yes | Onboarding → UStep7 → "I'll do it later" |
| 2026-06-14 | #239 / `0541c35` | feat | **Medication-reminder anchor (Anchor Recovery Phase 3).** The parent prompt's "Add medication reminder" CTA (was a Phase-2 log-only shell) now opens a smart-default sheet — morning 07:30 default + optional evening/before-bed dose, all-7-days, **no medication name stored** — that creates one standalone `tasks` row per dose (`self-care`, 5 credits, `is_system_generated`, 💊). OQ7 hides the CTA once a standalone meds task exists (Hebrew stem-match `תרופ`). No schema, no deps. Static gate + Hat-3 emulator (9/9, live DB) verified. Vibe-CTA task creation deferred to Phase 4. | Train | yes | Parent Dashboard → Anchor Recovery → Add medication reminder |
| 2026-06-15 | #244 / `c990784` | feat | **BUDDY friendship gift loop closed + L4/L5.** Gifts were granted on level-up but inert (carousel tap = hardcoded "coming soon" Alert; never marked used). Now: `use_buddy_gift` RPC (SECURITY DEFINER, family-ownership-checked) opens a gift → reveal modal, marks used, applies a cosmetic buddy theme color (`current_theme_color`), clears the pending dot; EOD climbs to L4 (30d) + L5 (100d); all gifts cosmetic-only v1. Migration **already live on DB** (function/RPC only). tsc clean; jest 402/402; RPC verified end-to-end on live DB with real parent auth (open / re-open-rejected / cross-family-rejected). **Merged post-vc47 cut → rides the NEXT train.** On-device UI tap-through pending (emulator flaky). | Train | yes | Child Gamer dashboard / 5A Me&Buddy / 5B My Stats → Boosters → open gift |
| 2026-06-16 | #248 / `9ce2ed8` | fix | **Parent Rewards: hide the target-size label when a reward has no size.** Catalog/legacy rewards with `size = null` rendered the raw i18n key `parentRewards.size.null` ("יעד parentRewards.size.null") as their target label (only small/medium/large keys exist). Now the goal line is omitted unless size is valid (cash rewards still show their cash badge). Surfaced via the Expo Web sweep; platform-agnostic (web + Android). tsc clean; jest 411; visually verified on the web build. | Train | yes | Parent Rewards → reward catalog rows |
| 2026-06-20 | #267 / `e3a7021` | feat | **PWA install nudge + Nudge Manager.** Parents on Android Chrome see an inline "Install" banner (calls Chrome's native install prompt + rich dialog with 6 screenshots); iOS Safari sees a 3-step Share ⬆ → Add to Home Screen instruction card; iOS other gets a "open in Safari" redirect. Settings → General gains an "Add to Home Screen" row (web-only). Shared Nudge Manager arbitrates at most one passive nudge slot at a time (priority + 7-day global cooldown); `pkg/rate-us-port` will consume this as a second consumer. Platform-split; zero Android regression (native no-op). tsc 0 · jest 18/18 new tests. Hat-4 device verify pending (confirm no banner/crash on native). | Train | yes | Parent Dashboard → install banner; Settings → General → Add to Home Screen |
| 2026-06-16 | #252 / `18dc5b1` | feat | **"Recommended now" parent card (Insights → action engine).** Turns the passive dashboard insight into a single one-tap action chosen by a transparent priority ladder: Pause→suppress · low-vibe→send warm sticker · lapse (unread `anchor_recovery`)→comeback (establish a med anchor, or reconnect) · streak≥3→celebrate. Reuses existing levers (sticker/med-reminder/bonus) + the `anchor_recovery` lapse signal; **retires the Anchor Recovery modal auto-show**; routes `anchor_recovery` notification taps to the dashboard. Purely additive — legacy insight card unchanged when no rec fires. tsc 0 · engine 12/12 · i18n guard 5/5 · bundle builds clean incl. card. **Hat-3 blocked by stale dev-client APK** (`SplashScreenManager` ClassNotFound — env, not this code); Hat-4 device pending. v2 defers live vibe-prompt / nudge / push / true auto-shrink. | Train | yes | Parent Dashboard → Recommended-now card; Notification → tap → dashboard |
| 2026-06-19 | #258 / `a39b5fa` | feat | **Per-task weekday selector on main + native time picker + pause-on-empty.** Forward-port of PR #233 (was stranded on `pkg/release-43`, never back-merged — `main` still hardcoded `schedule_days:[0-6]`, so the shipping app had NO parent day-selection). Parents now pick which weekdays each task runs (7 chips א׳–ש׳, default all-7); **deselecting ALL days pauses the task** (hidden from the child on every surface + excluded from yesterday-recap, with a "paused" hint to the parent). Free-text time field → native `DateTimePicker` (RTL/LTR by language, value forced LTR). DB: `tasks.schedule_days` now `DEFAULT '{0,1,2,3,4,5,6}' NOT NULL` (**already live**; 0 null/empty of 1121 before applying). Semantics single-sourced: `null`→every day, `[]`→no day. Gate 1 ✅ tsc 0 · jest 425/425. **Hat-3 blocked by the same stale dev-client APK** (`SplashScreenManager` ClassNotFound — env, not this code); device check pending. Merged post-vc48 cut → rides the train after 48. | Train | yes | Parent Tasks → Add/Edit task → day chips + time picker |
| 2026-06-19 | #260 / `ef144da` | feat | **Edit existing rewards + copy task/reward to another child.** Closes two Lovable parity gaps. ParentRewards: tap a catalog reward to edit it in place (emoji/title/size/credits, or cash) + delete with confirm (was create-only). New reusable `DuplicateToChildModal` (ported from Lovable) wired into ParentRewards (📋 per reward) and ParentTasks (📋 per task); shown only when the family has >1 child — inserts one fresh row per chosen sibling. No schema, no migration. Gate 1 ✅ tsc 0 · jest 425/425. Hat-3 device pending. | Train | yes | Parent Rewards → tap reward (edit) / 📋 copy ; Parent Tasks → 📋 copy |
| 2026-06-19 | #261 / `970ebc4` | fix | **Dev-client launch crash fixed — add missing `expo-splash-screen` (~31.0.13).** `expo-dev-launcher` referenced `expo.modules.splashscreen.SplashScreenManager` but the dep was absent → every locally-built dev client crashed on launch BEFORE JS loaded (mis-attributed to a stale APK in IN-2026-06-16). Production builds don't use the dev-launcher → testers unaffected. One dependency line + lockfile; no app code, no schema. **Unblocks emulator Hat-3 for all sessions.** Gate 1 ✅ (build verified; app launches to MainActivity, crash gone). | Train | no (devex) | (dev/build tooling — not user-facing) |
| 2026-06-23 | #276 / `9a06cc5` | feat | **Smart Capture — parent AI inbox → child task transfer.** Parents paste/photo/file a message from a teacher or activity coordinator; an AI stub parser extracts tasks/events; parent reviews + assigns to child(ren) with one tap. Supports one-time `due_date` tasks (new `tasks.due_date` column + migration). Platform-split file handling (Android FileSystem / Web FileReader). Phase-view and GamerTasksScreen updated to use `isTaskVisibleOn` (supports due-date filtering). tsc 0 · jest passing. Hat-3 + Hat-4 pending. | Train | yes | Parent Dashboard → capture icon → Smart Capture inbox |
| 2026-06-19 | #264 / `9b1d8cf` | feat | **Rich Parent Insights screen (Premium) + dashboard teaser & tappable entry.** The thin dashboard insight card now opens a full **Parent Insights screen** — weekly framing (real active-days + real week-over-week, no Math.random), strongest-phase / most-active highlights, a single reward-loop/completion tip, and a 7-day active/rest map; CTAs reuse the existing sticker/bonus/med/rewards levers. Dashboard entry made **visibly tappable** ("Full insights ›" cue, both active + locked variants), and free users now see a **real one-insight teaser** (value-first) with a "✨ Premium" badge → Paywall, instead of a bare lock box. i18n he+en; Premium-gated. Includes funnel-instrumentation backlog doc. | Train | yes | Parent Dashboard → insight card → Parent Insights screen |
| 2026-06-23 | `fix/insights-web-scroll` | fix | **ParentInsightsScreen web scroll fix** — `ScrollView` was missing `style={{ flex: 1 }}`; on Expo Web the container expanded to full content height and the page never scrolled. One-line fix, no schema, no migration. Android unaffected. | Train | yes | Parent Insights screen (web) |
| 2026-06-23 | `fix/onboarding-web-rtl` | fix | **Onboarding web RTL/LTR fixes** — `WelcomeScreen`, `_OnboardingShell`, `UStep5_Preview` all read `I18nManager.isRTL` (always `false` on web) instead of `useRTLStyles()` from `LanguageContext`. Back chevron, card emoji position, and text alignment were all LTR-locked on web for Hebrew users. Switched all three to `useRTLStyles()` so direction reads from the persisted language setting on both platforms. Also fixed he.json copy: `"הרוויח הרשאות ופרסים"` → `"הרשאות ופרסים"` (matches noun-phrase pattern of sibling motivator options). No schema, no migration. Android unaffected. | Train | yes | Onboarding flow (web, Hebrew) |
| 2026-07-03 | #310 / `2ebae5e` | feat | **Unified Rating & feedback screen — real stars + publish consent.** Replaces the sentiment-gated flow (a hidden hardcoded 2★) with one screen: required 1–5★ + optional note + explicit "you may publish my review" checkbox. **Consent (not star count) drives status** (`pending`→moderation / `private`). Collapses the two Settings rows ("Rate BUFF" + "Send feedback") into one **"Rating & feedback"**; admin FeedbackBoard shows private / awaiting-publish / published; Google Play CTA now offered to everyone post-submit (not sentiment-gated, more Play-compliant). Fixes a latent NOT-NULL insert on an empty note. tsc 0 (mobile + admin) · jest 9/9 · **on-device verified** (Android dev-client): single row, empty stars by default, consent toggles, Send gated on ≥1★. | Train | yes | Parent Settings → Rating & feedback → pick stars + consent → Send |
| 2026-07-08 | #331 / `e5edba0` | feat | **Landing `/summer` parent guide page (bilingual HE/EN).** Landing-site only — ships via Vercel redeploy of `buff-landing`, NOT the app train. | Train | yes (web) | buffadhd.com/summer renders both languages |
| 2026-07-08 | #332 / `f4c1b48` | fix | **"I'm a Parent" goes to Signup (role preselected), not Login.** Removes the dead-end for brand-new parents. QA-sweep verified (web). | Train | yes | RoleSelection → "I'm a Parent" → Signup screen |
| 2026-07-08 | #333 / `cf7ea67` | fix | **No late-shame: neutral timestamp instead of warning badge on late-completed tasks** (was alert-icon + hardcoded EN "outside window" — Pillar 2 violation). QA-sweep verified (device, 15:10 morning-task completion). | Train | yes | Child completes a task outside its phase window → normal done row, no warning |
| 2026-07-08 | #334 / `bf3d57b` | fix | **Rewards fetch error shows a distinct retryable error state in Child+Gamer shops** (was masquerading as "no rewards yet" empty state on flaky networks). | Train | yes | Child Shop with fetch error → error+retry, not empty state |
| 2026-07-08 | #335 / `10aee59` | fix | **Gamer daily loop: server-sourced successful-days, 0-streak hidden ("Start one today"), full-row task tap, tappable preview-exit banner.** QA-sweep verified all four on device (DB 6 = UI 6; tap toggles 140↔175 Buffs). | Train | yes | Gamer HQ + Quests: stat parity, streak card, row tap, banner exit |
| 2026-07-08 | #336 / `a665a7b` | fix | **Child success re-anchored to absolute count (D-2026-06-14) — fuel bar 0/goal, egg hatch, ignition copy** ("{{goal}} done = Ignition!"). QA-sweep verified (Mint + Gamer). | Train | yes | Child dashboard Focus Fuel shows 0/3 + count goal hint |
| 2026-07-08 | #337 / `8b98b29` | fix | **Purchase screens removed from child stack + role guard + Paywall legal links wired** (`buff://founding-100` on a child device falls back to ChildApp; Privacy/Terms now open — Play compliance). 7 new Jest tests. | Train | yes | Child deep-link founding-100 → ChildApp; Paywall footer links open |
| 2026-07-08 | #338 / `aa23f25` | fix | **Parent dashboard invite card uses cross-platform `shareInvite`** (web was a silent `Share.share` no-op — the web-activation villain). QA-sweep verified: Android share sheet; web clipboard fallback + "הועתק" toast. | Train | yes | Dashboard invite card → Share on Android + Web |
| 2026-07-08 | #339 / `ec440dd` | fix | **Web dead controls → real pickers: platform-split time/date fields in ParentTasks, EditChild, Activities** (+ MedReminder). QA-sweep verified live `input[type=time]` save round-trip on web. 4 new Jest picker suites. | Train | yes | (web) Parent Tasks edit → time input saves; EditChild birthday |
| 2026-07-08 | #340 / `f8ea774` | fix | **Self-destroying service worker evicts the legacy Lovable PWA worker** on buffadhd.com. Landing-site only — ships via Vercel redeploy, NOT the app train. | Train | yes (web) | Old-PWA browsers get fresh landing after SW self-destruct |
| 2026-07-08 | #341 / `f2001ef` | fix | **QA sweep follow-ups: parent dashboard + Philosophy copy aligned to count-based active-day rule** (`successDay.ts`; "70% = a successful day" removed everywhere, `weeklyGoal.goalCount` EN+HE) + ThemeContext Jest suite fixed & preview-theme test un-quarantined (666/666, 0 skips). Copy redlined by Adi at merge. | Train | yes | Parent dashboard child card shows "{{goal}} done = an active day"; Philosophy pillar 6 |

> 🚧 **CUT — 1.8.0 (versionCode 66), 2026-07-09.** Content = the 2026-07-08 rows above that are app-code: **#332 · #333 · #334 · #335 · #336 · #337 · #338 · #339 · #341** (+ CI workflows #328/#329 ride along, no runtime content). **Landing-only rows (#331, #340, + #343) ship via Vercel, NOT this build.** Manifest + notes: `docs/releases/66/`. Gate 1 ✅ tsc 0 · jest 666/666 (0 skips). Gate 2 ✅ = the full 2026-07-08 QA sweep (MASTER_TEST_PLAYBOOK § Run 2026-07-08). Build: `EAS Build (Android)` workflow from `origin/main`, autoIncrement → vc66. Rows move to **Shipped (66)** after Adi promotes + "verified, tag it".

> 🚉 **NEXT BUILD — merged to `main` AFTER the 1.8.0(vc66)/1.8.1(vc67) train, NOT in any build yet.** Activates on whichever binary is next cut from `origin/main`; no cut-time action needed for it to take effect (config already in `main`).
>
> | Date merged | PR / Commit | Type | Change (native-relevant) | Lane | User-facing? | Flow Suite |
> |---|---|---|---|---|---|---|
> | 2026-07-11 | #350 / `6244b43` | feat | **Activate OTA (EAS Update)** — JS-only changes now ship over-the-air (minutes, no Play review) via `checkAutomatically:ON_LOAD` + `runtimeVersion:fingerprint`. Config-only, no app-code, no new dep. **⚠️ The FIRST binary carrying this config must ship through the store once — OTA works from that binary onward; the currently-live binary cannot receive OTA.** At cut: confirm fingerprint builds clean (the one risk), then `ota:preview`→verify→`ota:prod`. Playbook: `docs/OTA_PLAYBOOK.md`; tests: `docs/sessions/eas-update-ota/TESTS.md`. | Train | no (infra; makes JS updates invisible-fast) | OTA: preview build → `eas update --branch preview` → new JS applies on 2nd cold start (TESTS Hat 3.3) |
> | 2026-07-12 | #353 / `7d5f600` | fix | **Un-complete guard (Safe Harbour):** 2s post-completion tap lock + friendly confirm before un-completing debits Buffs (Pastel task cards). | Train | yes | Complete a task → immediate 2nd tap ignored; later tap asks "Undo this one?" |
> | 2026-07-12 | #354 / `ebb27dc` | fix | **Auth errors + form ergonomics:** network failures no longer masquerade as "Invalid email or password"; password-reset failure feedback; keyboard next-chaining + show/hide password on Login+Signup. | Train | yes | Login with wifi off → connection error (not invalid-credentials) |
> | 2026-07-12 | #355 / `edfad89` | feat | **Always-close-to-a-win on child rewards:** per-card progress bar + inline "עוד X!" (no blocking alert), Buffs price always shown to child (₪ tag parent-only), ≥44pt targets in both skins. | Train | yes | Child Shop: unaffordable reward shows progress + x-to-go inline |
> | 2026-07-12 | #356 / `de4bfaf` | fix | **i18n sweep:** parent-screen hardcoded strings → t() (subscription status, Insights ternaries, Buffs labels), PhaseView empty state in buddy voice, `category.other` added, dead `ignition.*` (18 keys) removed. | Train | yes | Hebrew locale: Settings subscription status + Insights render in Hebrew |
> | 2026-07-12 | #357 / `1384d37` | fix | **Parent dashboard ergonomics:** dirty-guard on bonus/sticker/task-sheet backdrops (typed text no longer lost), pull-to-refresh, time-aware greeting. | Train | yes | Type a bonus note → tap backdrop → "Discard?" confirm; pull down refreshes |
> | 2026-07-12 | #358 / `4c4cb07` | fix | **Pet card honors the child's buddy rename** (was hardcoded 'Buddy'); i18n'd default. | Train | yes | Rename buddy in Settings → HQ pet card shows the new name on return |
> | 2026-07-12 | #359 / `45453f8` | fix | **Gamer polish:** unparseable-time tasks visible under every filter (no vanishing quests), HQ completion pop + haptics (setting wired), gear→Settings / dead bell removed, chip a11y, credits label i18n. | Train | yes | Gamer HQ: filter to Evening → time-less task still listed; complete → pop |
> | 2026-07-12 | #361 / `0045ab1` | feat | **Parent dashboard success = count goal (D-2026-06-14):** `done/goal ⚡` badge replaces `%` (amber gone; neutral below goal, green at goal), "~3 tasks = a successful day" line. Companion spec-sync: BUFF_VALUES.md updated in #360 (docs-only, no build impact). | Train | yes | Parent dashboard child card shows "2/3 ⚡" + count goal line, no % |
> | 2026-07-12 | #352 / `18d34a2` | feat | **Play flexible in-app-update prompt on launch** (Noa gap) — new native dep `expo-in-app-updates@^0.12.0` (Adi-approved), lazy-required, Android-only, web no-op. | Train | yes | Installed older binary + newer on track → dismissible update sheet on launch |
> | 2026-07-12 | #362 / `93b859a` | chore | Buddy default-name key unified to `pet.defaultName` (dup from parallel #358/#359 removed). | Train | no | — |
>
> 🚧 **CUT — 1.8.2 (versionCode 68), 2026-07-12.** vc67 (1.8.1) published to Play 2026-07-12 → this table departs. Content = ALL rows above (#350 OTA activation · #352 in-app-update prompt · #353–#359, #361–#362 UX batch 2). **This is the FIRST OTA-capable binary** — after it's live, JS-only fixes ship via `ota:preview`→`ota:prod` (docs/OTA_PLAYBOOK.md); build-time risk = fingerprint runtime resolving clean. Manifest: `docs/releases/68/MANIFEST.md`. Gate 1 ✅ tsc 0 · jest 732/732 (82 suites, 0 skips) · key-check ✅ (on `93b859a`). Build: `EAS Build (Android)` workflow from `origin/main`, autoIncrement → vc68. Rows move to **Shipped (68)** after Adi promotes + "verified, tag it".

**Riding the next train — AFTER the vc68 cut (merged post-cut, NOT in vc68):**

| Date merged | PR / Commit | Type | Change (one-liner) | Lane | User-facing? | Flow Suite |
|---|---|---|---|---|---|---|
| 2026-07-14 | #364 / `48336e3` | fix | **Buddy name read/write scoped to the active child everywhere** — no parent-name leak; rename propagates. | Train | yes | Rename buddy → name shown consistently across child screens |
| 2026-07-15 | #365 / `b1258cf` | feat | **AI coach insight on the Parent Dashboard** (replaces the rule-based %-card as primary): headline/message/action + CTA + 👍👎 vote + "As of {date}" stamp; shared `useAutoCoachInsight` auto-generate guards; rule-based card stays as fallback, free-Android teaser unchanged. Server side already LIVE (edge fn v15: **Gemini primary** + fallback until `GEMINI_API_KEY` secret is set; migration 042 `computed_at`). **JS-only client change → OTA-eligible** once vc68 is the live binary (docs/OTA_PLAYBOOK.md); web lands on Vercel redeploy. **Pushed to production OTA 2026-07-15** (group `3ce1b823`, runtime `056b9b94`, from `b1258cf`). | Train | yes | Parent dashboard (entitled/trial or web) → AI coach card renders with date stamp; 👍/👎 persists; tap → Insights screen |
| 2026-07-17 | #367 / `6831c8f` | fix | **Smart-insight UX polish:** start-conversation CTA renders as quiet 💬 text instead of a dead button (Insights screen); trial ribbon Hebrew pluralization ("נותר יום אחד" not "נותרו 1 ימי"). JS-only → OTA-eligible. | Train | yes | Insights: insight with start-conversation shows text, no tappable button · dashboard ribbon at 1 day reads correctly |
| 2026-07-17 | #371 / `cd43cfc` | fix | **Dashboard AI card refreshes on focus + pull-to-refresh** — a generate/vote on the Insights screen no longer leaves a stale insight/"as of" date on the mounted dashboard tab. | Train | yes | Generate on Insights → back to dashboard → card + date update without restart |
| 2026-07-17 | #372 / `e9497b6` | chore | **OTA publishes move to CI** (`.github/workflows/eas-update.yml`) — fingerprint parity with CI-built binaries. See Lesson 2026-07-17. | Train | no (infra) | — |
| 2026-07-28 | #396 / `f4ac672` | feat | **One-time email opt-in ask for OAuth parents** (lifecycle-emails Phase 2). Google-OAuth signups never saw the signup checkbox, so `AuthCallbackScreen` hardcoded `marketing_consent:false` — measured 2026-07-27, that left **37 of 43 reachable parents unmailable** and capped the legitimate audience at 6. A sheet on the parent dashboard asks once, in the parent's own language, reusing `auth.marketingConsent` verbatim; **both** answers are stamped so a NO is never re-asked. Suppressed under `isChildPreview`. Asked-stamp stored in `pro_settings.marketing_consent_asked_at` → **no migration**. JS-only. | OTA | yes | Existing OAuth parent → sheet appears once → answer → never returns (incl. after relaunch) · tap No → consent stays false, stamp written · **View-as-Child → sheet must NOT appear** · same on web |
| 2026-07-27 | #395 / `d2a6bfa` — `pkg/community-surfacing` | feat | **The app finally points parents at the WhatsApp community.** The `philosophy.community.*` copy existed and was translated in both languages, but no component ever rendered it (IN-2026-07-27-01) — so the app had **zero** community surfaces and joins had flatlined. Adds 4: Settings → About row · Philosophy screen block · a one-shot quiet link at the end of first-time onboarding (suppressed on add-child, below the CTA) · a block in **both** language sections of all 4 SEO guides. The group is chosen by the resolved i18n value, so there is no locale branching anywhere. New `src/platform/openExternalUrl` split (web must open synchronously or the popup blocker eats it). Click-out logged via `logOnboardingEvent` as `community_link_clicked` + placement — **no migration** (`event_type` carries no CHECK). JS + static HTML only, no new deps. | OTA | yes | Settings → BUFF Community → correct-language group opens · switch app language → other group · add-child → invite absent · guides → community block at the foot of both languages |

> ⚠️ **OTA correction (2026-07-17):** the locally-published OTAs of 2026-07-12 (#364, group `caf352b5`) and 2026-07-15 (#365, group `3ce1b823`) **never reached any device** — local runtime fingerprint `056b9b94` ≠ vc68 binary's `e4983c15` (silent mismatch; Lesson 2026-07-17 in INTEGRATION_LEARNINGS). **First OTA that actually delivers: 2026-07-17, group `852d44c2`** (runtime `e4983c15` ✅, published from CI via the new workflow, content = #364+#365+#367+#371, from `e9497b6`).

**Note on the version numbers:** EAS auto-incremented past 35/36/37 (parallel builds); build **38** (1.4.1) was cut then canceled; the live build **39** (1.4.1) carries all rows above. The versionCode is just a monotonic counter — content is what matters.

### 📣 Post-ship notifications — tell the user when it lands
- **Tamar** — co-parent join (PR #179): she asked whether her partner can join with his own Google + the family code. **When the build carrying #179 ships to Play, message her** that it's live + the how (partner signs in with Google → Settings → "Join Family" → enter family code). Draft ready (2026-06-06). Until then she can't do it on her installed mobile build.

### Departure check (proposed cut — 2026-06-05)
- Days since last release: **2** (1.2.0(28) cut 2026-06-03) — below the ~14d trigger
- User-facing items queued: **3** — 1 fix (#157) + 2 features (#159 child-login, #161 notif feed)
- **Recommendation:** _content-ready (≥1 notable feature trigger met, ×2), verification-gated._ Cut the next versionCode once Gate-2 functional smoke is green on #157/#159/#161; #159 Hat-4 (real device) remains the only open device check.

---

## ✅ Shipped — drained into past releases

Newest first. Each block = one release the queue fed.

### 1.4.0 (versionCode 34) — Alpha closed-testing, promoted 2026-06-08
Lane mix: all Train · Manifests: `docs/releases/34/MANIFEST.md` (features cut from `main`) + `docs/releases/31/MANIFEST.md` (the 1.3.1 fixes train, released internal 2026-06-06).
Build: EAS `a266ea2f` (1.4.0, versionCode 34). versionCode 32 + 33 burned/superseded en route. The 11 PRs in the 34 manifest stacked on the 1.3.1(31) content; the fix rows below shipped first via 31, the rest landed in 34.

| PR / Commit | Type | Change | User-facing? |
|---|---|---|---|
| #157 / `62e31bd` | fix | Parent notification bell clears the screen title in Hebrew RTL | yes |
| #159 / `878ea96` | feat | Child login by pick-from-list keyed on immutable profile id (no dup accounts) | yes |
| #161 / `df0719b` | feat | Parent notification bell unread-only "show-new" feed | yes |
| #165 / `ab6f3f2` | feat | Kids redeem rewards with parent approval; atomic BUFF deduct | yes |
| #170 / `bcdb8cb` | fix | Cash-reward currency symbol follows app language (Hebrew → ₪) | yes |
| #173 / `4a1f99e` | fix | Notification bell inline header element + compact "+" action | yes |
| #174 / `5c7ce63` | fix | English parent link-child sheet in English (6 hardcoded HE strings) | yes |
| #177 / `pkg/child-vault-write-rls` | fix | Own-device kids' BUFFs persist (surface credit_vault write errors) | no (guard) |
| #178 / `c662836` | fix | Sticker/Bonus bottom sheet lifts cleanly above keyboard | yes |
| #179 | feat | Second parent joins existing family via family code; family-wide premium | yes |
| `fix/duplicate-child-guard` | fix | Atomic create_child_profile + friendly duplicate dialog; delete RPC fix | yes |
| #189 / `530bc39` | fix | Parents can edit own-device kids (migration 022); real buddy in menu | yes |
| #191 / `e2e8590` | fix | Show child's real name (not "Preview") in View-as-Child on mint dashboard | yes |
| #192 / `85f6ca6` | feat | Capture `families.platform` at signup (funnel segmentation) | no (instrumentation) |
| #190 / `6163e6b` | feat | iOS Phase-1: hide paywall + skip RevenueCat init; enable iOS EAS builds | yes (iOS) |
| #186/#184 / `e6bb935` | feat | Admin Tester Board (internal tool) | no (admin) |
| #188 / `f991c11` | feat | Admin: parent email as mailto link | no (admin) |
| #185 / `356791a` | fix | credit-vault atomic balance adjustment (kill read-modify-write race) | yes |
| #182 / `a311b72` | fix | Child HQ screens day-filter like the Quests tab | yes |
| #187 / `864f771` | fix | Admin funnel per-stage label "at this stage" | no (admin) |
| #183 / `46938db` | chore | buff-emulator skill — shared emulator/Metro lease lock (devex) | no |

### V25 — versionName 1.1.1 (internal, ~2026-05-31)
_Pre-protocol baseline. Future releases list their drained queue rows here._
- (historical — see `STATUS` / `docs/releases/` once per-release folders exist)

<!--
Template for a new shipped block:

### V<N> — versionName <x.y.z> (<track>, <date>)
Lane mix: <X Train, Y Hotfix> · Manifest: docs/releases/v<N>/MANIFEST.md
| PR/Commit | Type | Change | User-facing? | Gate2 verdict |
|---|---|---|---|---|
| #NNN | fix | ... | yes | ✅ F7.H2 |
-->

---

**Maintained by:** CC (rows at merge time) · Adi (cut approval).
**Last updated:** 2026-07-12 (vc68 cut)
